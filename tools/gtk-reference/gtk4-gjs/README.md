# GTK4 GJS Reference App

Reference scenes for baseline GTK4 widgets used for Venner parity checks.

## Run

```bash
cd tools/gtk-reference/gtk4-gjs
gjs -m app.js
```

## Scenes

- Button
- Entry
- Switch
- Tabs (implemented with `Gtk.Stack` + `Gtk.StackSwitcher`)
