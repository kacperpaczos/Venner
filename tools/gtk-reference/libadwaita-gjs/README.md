# Libadwaita GJS Reference App

Reference scenes for Libadwaita widgets used for Venner parity checks.

## Run

```bash
cd tools/gtk-reference/libadwaita-gjs
gjs -m app.js
```

## Scenes

- Button
- Entry
- Switch (`Adw.SwitchRow`)
- Tabs (implemented with `Adw.ViewStack` + `Adw.ViewSwitcher`)
